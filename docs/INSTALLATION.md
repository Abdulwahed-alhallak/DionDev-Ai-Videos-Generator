# 📘 دليل التثبيت - Installation Guide

## 📋 المتطلبات الأساسية - Prerequisites

قبل البدء بتثبيت النظام، تأكد من توفر المتطلبات التالية:

### البرمجيات المطلوبة:
- **Node.js** >= 20.0.0 ([تحميل](https://nodejs.org/))
- **PostgreSQL** >= 16.0 ([تحميل](https://www.postgresql.org/download/))
- **npm** أو **yarn** (يأتي مع Node.js)
- **Git** ([تحميل](https://git-scm.com/))

### اختياري (للتطوير):
- **pgAdmin** لإدارة قاعدة البيانات
- **Postman** لاختبار API
- **VS Code** أو محرر نصوص مفضل

---

## 🚀 خطوات التثبيت

### 1️⃣ استنساخ المشروع

```bash
git clone https://github.com/your-repo/dar-abi-alfadaa-system.git
cd dar-abi-alfadaa-system
```

---

### 2️⃣ إعداد قاعدة البيانات

#### أ. إنشاء قاعدة البيانات

```bash
# الدخول إلى PostgreSQL
psql -U postgres

# إنشاء قاعدة البيانات
CREATE DATABASE dar_abi_alfadaa_db;

# إنشاء مستخدم جديد (اختياري)
CREATE USER dar_admin WITH PASSWORD 'your_secure_password';
GRANT ALL PRIVILEGES ON DATABASE dar_abi_alfadaa_db TO dar_admin;

# الخروج
\q
```

#### ب. تشغيل Schema

```bash
# تشغيل ملف Schema
psql -U postgres -d dar_abi_alfadaa_db -f database/schema.sql
```

#### ج. التحقق من إنشاء الجداول

```bash
psql -U postgres -d dar_abi_alfadaa_db

# عرض الجداول
\dt

# يجب أن ترى 20 جدول
```

---

### 3️⃣ تثبيت التبعيات

#### Frontend (React)

```bash
cd client
npm install
```

#### Backend (Node.js)

```bash
cd ../server
npm install
```

---

### 4️⃣ إعداد متغيرات البيئة

#### أ. نسخ ملف المثال

```bash
# من المجلد الرئيسي
cp .env.example .env
```

#### ب. تعديل الملف `.env`

افتح ملف `.env` وقم بتعديل القيم:

```env
# قاعدة البيانات
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dar_abi_alfadaa_db
DB_USER=postgres
DB_PASSWORD=your_password_here

# JWT
JWT_SECRET=your_very_strong_secret_key_here_min_32_chars
JWT_EXPIRY=24h

# الخادم
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:3000

# البريد الإلكتروني (اختياري في البداية)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_email_password
```

**⚠️ مهم:** 
- استخدم كلمة مرور قوية لقاعدة البيانات
- أنشئ JWT_SECRET قوي (32 حرف على الأقل)
- لا تشارك ملف `.env` أبداً

---

### 5️⃣ تشغيل النظام

#### أ. تشغيل Backend

```bash
cd server
npm run dev
```

يجب أن ترى:

```
==============================================
🕌 نظام إدارة دار أبي الفداء للعلوم الشرعية
==============================================
🚀 الخادم يعمل على المنفذ: 5000
🌐 الرابط: http://localhost:5000
📚 API: http://localhost:5000/api
🔍 Health Check: http://localhost:5000/api/health
==============================================
```

#### ب. تشغيل Frontend (في نافذة طرفية جديدة)

```bash
cd client
npm run dev
```

يجب أن ترى:

```
VITE v5.1.3  ready in XXX ms

➜  Local:   http://localhost:3000/
➜  Network: use --host to expose
```

---

### 6️⃣ فتح التطبيق

افتح المتصفح وانتقل إلى:

```
http://localhost:3000
```

يجب أن تظهر صفحة تسجيل الدخول.

---

## 🧪 الاختبار

### اختبار الاتصال بقاعدة البيانات

```bash
cd server
npm run dev
```

إذا رأيت `✅ تم الاتصال بقاعدة البيانات بنجاح`، فالاتصال يعمل بنجاح.

### اختبار API

افتح المتصفح أو Postman وانتقل إلى:

```
GET http://localhost:5000/api/health
```

يجب أن تحصل على:

```json
{
  "status": "OK",
  "message": "نظام إدارة دار أبي الفداء للعلوم الشرعية يعمل بنجاح",
  "timestamp": "2025-XX-XXTXX:XX:XX.XXXZ"
}
```

---

## 🔧 إعدادات إضافية

### إضافة بيانات تجريبية (اختياري)

```bash
cd server
npm run seed
```

هذا سيضيف:
- 20-30 طالب
- 10-15 مدرس
- 15-20 مقرر
- بيانات تجريبية أخرى

### تشغيل في وضع الإنتاج

#### Frontend

```bash
cd client
npm run build
npm run preview
```

#### Backend

```bash
cd server
npm run build
npm start
```

---

## 🐳 التشغيل باستخدام Docker (اختياري)

إذا كان لديك Docker و Docker Compose مثبتين:

```bash
# من المجلد الرئيسي
docker-compose up -d
```

هذا سيقوم بـ:
- إنشاء حاوية PostgreSQL
- إنشاء حاوية Backend
- إنشاء حاوية Frontend
- ربطهم معاً

---

## 🔍 استكشاف الأخطاء

### مشكلة: لا يمكن الاتصال بقاعدة البيانات

**الحل:**
1. تحقق من تشغيل PostgreSQL: `pg_isready`
2. تحقق من صحة بيانات الاتصال في `.env`
3. تحقق من صلاحيات المستخدم

```bash
psql -U postgres
\du
```

### مشكلة: خطأ في تثبيت التبعيات

**الحل:**
```bash
# حذف المجلدات وإعادة التثبيت
rm -rf node_modules package-lock.json
npm install
```

### مشكلة: المنفذ 5000 أو 3000 مستخدم

**الحل:**
- غير PORT في `.env` للـ Backend
- غير port في `client/vite.config.ts` للـ Frontend

### مشكلة: خطأ CORS

**الحل:**
تحقق من تطابق CLIENT_URL في `.env` مع عنوان Frontend.

---

## 📱 الوصول من أجهزة أخرى على الشبكة المحلية

### Backend

في ملف `server/src/server.ts`، غير:

```typescript
app.listen(PORT, '0.0.0.0', () => {
  // ...
});
```

### Frontend

في `client/vite.config.ts`:

```typescript
server: {
  host: '0.0.0.0',
  port: 3000,
}
```

ثم يمكنك الوصول عبر IP الجهاز:
```
http://192.168.1.XXX:3000
```

---

## 🔐 الأمان في الإنتاج

عند النشر في بيئة الإنتاج:

1. **استخدم HTTPS:**
   - احصل على شهادة SSL (مثل Let's Encrypt)
   - استخدم nginx كـ reverse proxy

2. **قواعد البيانات:**
   - غير كلمة المرور الافتراضية
   - استخدم مستخدم مخصص بصلاحيات محدودة
   - فعّل SSL للاتصال بقاعدة البيانات

3. **متغيرات البيئة:**
   - لا تستخدم القيم الافتراضية
   - استخدم JWT_SECRET قوي وفريد

4. **Firewall:**
   - أغلق المنافذ غير الضرورية
   - اسمح فقط بالاتصالات المطلوبة

---

## 📞 الدعم

إذا واجهت أي مشاكل:

1. راجع قسم استكشاف الأخطاء
2. تحقق من السجلات (logs)
3. ابحث في Issues على GitHub
4. اتصل بالدعم الفني

---

## ✅ الخطوات التالية

بعد التثبيت الناجح:

1. ✅ سجل دخول باستخدام حساب المدير الافتراضي
2. ✅ غير كلمة المرور
3. ✅ أضف بيانات النظام الأساسية
4. ✅ أنشئ حسابات المستخدمين
5. ✅ ابدأ بإدخال البيانات

---

<div align="center">

**بالتوفيق في استخدام النظام! 🎓**

**وَقُل رَّبِّ زِدْنِي عِلْمًا**

</div>
